import { makeStyles } from '@mui/styles';

export default makeStyles(theme => ({
    container: {
      size:"small",
      top: 0,
      left: 0,
      background: theme.palette,
    },
    cabecera:{
      size:'small',
      background:theme.palette,
    },
    
}));